# Aurealize Cards
Slop ai but works

## k3s

The Podman-built production container serves the Vue app and Elysia API together on port `3000`.

Deployment files and setup instructions are in [`deploy/k3s`](deploy/k3s/README.md). The configured production origin is `https://aurealize.aureal.dev`.
